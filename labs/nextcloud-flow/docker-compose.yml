# Nextcloud AIO for Flow Lab
# Usage: docker compose up -d
# Access AIO: https://localhost:8180
# Access Nextcloud: https://localhost.local (port 443)

services:
  nextcloud-aio-mastercontainer:
    image: nextcloud/all-in-one:latest
    init: true
    restart: always
    container_name: nextcloud-aio-mastercontainer
    volumes:
      - nextcloud_aio_mastercontainer:/mnt/docker-aio-config
      - /var/run/docker.sock:/var/run/docker.sock:ro
    ports:
      - "8180:8080"
    environment:
      - APACHE_PORT=11000
      - APACHE_IP_BINDING=0.0.0.0
      - NEXTCLOUD_DATADIR=/mnt/ncdata
      - SKIP_DOMAIN_VALIDATION=true

  nginx-proxy:
    image: nginx:alpine
    container_name: nextcloud-nginx-proxy
    restart: always
    ports:
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl.crt:/etc/nginx/ssl/ssl.crt:ro
      - ./nginx/ssl.key:/etc/nginx/ssl/ssl.key:ro
    networks:
      - default
      - nextcloud-aio

volumes:
  nextcloud_aio_mastercontainer:
    name: nextcloud_aio_mastercontainer

networks:
  nextcloud-aio:
    name: nextcloud-aio
    external: true
